fragment PaginationProp on Pagination {
    hasNext
    next
}

fragment PhotoProp on Photo {
    id
    ownerName
    location {
        latitude
        longitude
        accuracy
    }
    photoUrls {
        size
        url
        width
        height
    }
}